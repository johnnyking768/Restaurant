/*
 * Paystack checkout configuration.
 *
 * Paste your Paystack TEST public key below. It begins with `pk_test_`.
 * Never paste a secret key (`sk_test_` or `sk_live_`) into this file: browser
 * code is public and Paystack secret keys belong only on a secure server.
 */
window.GoldenPlatePaystack = {
  publicKey: "pk_test_9fd36c5c00120536c49b3ac0f3d28c3a41705696"
};


// The best next upgrade is an admin order dashboard
//  where you can see each order, verify payment, and 
//  update those four statuses. Then customers could 
//  check their order status from a link or WhatsApp.